import React from 'react'
import { Box } from "@theme/components/Box";
import { colors } from "@theme/styles/colors";

const Nav = () => {
    return (
        <Box position="sticky" width="100%" height="70px" backgroundColor={`${colors.primaryText}`}>
           
        </Box>
    )
}

export default Nav
